using Godot;

public partial class LevelTemplate : Node2D
{
    // Called when the node enters the scene tree for the first time.
    //public override void _Ready()
    //{
    //    TileMap tileMap = GetNode<TileMap>(nameof(TileMap));
    //    Camera2D camera = GetNode<Player>(nameof(Player)).GetNode<Camera2D>(nameof(Camera2D));
    //    camera.LimitRight = tileMap.GetUsedRect().End.X * tileMap.TileSet.TileSize.X + tileMap.TileSet.TileSize.X;
    //}
}
